{
  "name": "ghostpipe",
  "description": "Alternative YT frontend with small api changes",
  "dependencies": {
    
  },
  "scripts":{
    "heroku-prebuild": "bash install_binaries.sh",
    "heroku-postbuild": "npm install ghostpipe-backend && npm install ffmpeg-static@4.2.4"
  }
}
