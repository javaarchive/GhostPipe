{
  "name": "ghostpipe",
  "description": "Alternative YT frontend with small api changes",
  "dependencies": {
    
  },
  "scripts":{
    "heroku-prebuild": "bash install_binaries.sh",
    "heroku-postbuild": "cd ghostpipe-backend && npm install && npm install ffmpeg-static@4.2.4"
  }
}
